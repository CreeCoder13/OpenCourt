import type { Metadata } from "next";
import { Breadcrumbs, PageShell } from "../../components/SiteChrome";
import { AdminReviewQueue } from "../../components/AdminReviewQueue";
import { sourceProviders } from "../../data/sourceRegistry";

export const metadata: Metadata = { title: "Admin Access | OpenCourt", robots: { index: false, follow: false } };
const modules = ["Cases", "Sources", "Related cases", "Communities", "Treaties", "Legal topics", "Active litigation", "Quotations", "Timeline events"];
export default function AdminPage() { return <PageShell><section className="admin-page"><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Admin" }]} /><div className="admin-intro"><span>Restricted research workspace</span><h1>Indigenous legal discovery</h1><p>Review source-backed candidates, resolve duplicates, and publish only records with authoritative verification.</p></div><AdminReviewQueue /><div className="admin-architecture"><div><h2>Editorial modules</h2><p>Durable records, source evidence and case relationships share one protected review workflow.</p></div><div className="admin-module-grid">{modules.map((module) => <div key={module}><span>＋</span><b>{module}</b><small>Add · Edit · Review</small></div>)}</div><section className="ingestion-console"><div><p className="section-number">Source intake</p><h2>National discovery engine</h2><p>Deterministic filters run before AI. Every candidate remains unpublished until the verification gate and an editor both approve it.</p></div><div className="ingestion-provider-list">{sourceProviders.map((provider) => <div key={provider.id}><span className="provider-status" /><div><b>{provider.name}</b><small>{provider.discoveryMethod} · {provider.authority}</small></div><em>Review required</em></div>)}</div></section><div className="workflow-row"><div><span>1</span><b>Discovered</b><small>Robots-checked and cached</small></div><i>→</i><div><span>2</span><b>Verified</b><small>Primary-source evidence attached</small></div><i>→</i><div><span>3</span><b>Published</b><small>Explicit editorial approval</small></div></div><div className="ai-safeguard"><b>Human review is mandatory</b><p>AI can classify, extract and summarize, but cannot mark legal facts verified. Unverified records are blocked from publication at the server.</p></div></div></section></PageShell>; }
