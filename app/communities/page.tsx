import type { Metadata } from "next";
import Link from "next/link";
import { Breadcrumbs, PageShell, VerificationBadge } from "../../components/SiteChrome";
import { communities } from "../../data/catalog";

export const metadata: Metadata = { title: "Community Profiles | OpenCourt", description: "Find verified court cases connected to First Nations, Métis, and Inuit communities and organizations." };
export default function CommunitiesPage() { return <PageShell><section className="page-hero"><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Indigenous Communities" }]} /><div className="page-hero-grid"><div><p className="kicker">Indigenous community index</p><h1>Indigenous communities</h1></div><p>Browse dedicated profiles and verified case relationships for First Nations, Métis and Inuit communities or organizations. Treaty relationships are shown only where recorded; no numbered-treaty connection is assumed.</p></div></section><section className="catalog-section"><div className="directory-toolbar"><p><b>{communities.length}</b> community profiles</p><span>First Nations · Métis · Inuit</span></div><div className="community-grid">{communities.map((community) => <Link href={`/communities/${community.slug}`} key={community.id}><div className="community-card-top"><span className="community-initial">{community.name.charAt(0)}</span><VerificationBadge level={community.verificationLevel} /></div><h2>{community.name}</h2><p>{community.indigenousGroup} · {community.provinceTerritory}</p><dl><div><dt>Connected cases</dt><dd>{community.caseSlugs.length}</dd></div><div><dt>Treaty</dt><dd>{community.treaties.join(", ") || "None recorded"}</dd></div></dl><span className="card-link-inline">View community page →</span></Link>)}</div></section></PageShell>; }
