import type { Metadata } from "next";
import { Breadcrumbs, PageShell } from "../../components/SiteChrome";

export const metadata: Metadata = { title: "Cases to Watch | OpenCourt", description: "Track verified ongoing Indigenous-rights litigation and appeal stages." };
const stages = ["Filed", "Hearing Scheduled", "Decision Reserved", "Appeal Filed", "Appeal Pending", "Supreme Court Leave Application", "Supreme Court Hearing", "Awaiting Judgment"];
export default function CasesToWatchPage() { return <PageShell><section className="page-hero watch-hero"><Breadcrumbs items={[{ label: "Home", href: "/" }, { label: "Cases to Watch" }]} /><div className="page-hero-grid"><div><p className="kicker">Active litigation tracker</p><h1>Cases to watch</h1></div><p>Ongoing matters change quickly. An entry is published only when its current stage, last court event, and last verified date have a reliable source.</p></div></section><section className="watch-section"><div className="watch-summary"><div><span className="watch-pulse" /><div><b>No tracked cases published</b><p>The Phase 1 interface is ready; future dates will never be estimated or invented.</p></div></div><span>Last checked 26 August 2026</span></div><div className="watch-empty"><span>◷</span><h2>Verified active matters will appear here</h2><p>Each record will show the case name, court, Indigenous party, other parties, main issue, current stage, last court event, next known event, last verified date, and sources.</p><a href="/sources">Read the verification methodology →</a></div><div className="stage-legend"><h2>Supported tracking stages</h2><div>{stages.map((stage) => <span key={stage}>{stage}</span>)}</div></div></section></PageShell>; }
